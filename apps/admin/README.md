# ObserverNet Admin Portal

Next.js 15 dashboard for Org Admins and Election Managers. Supports election creation, policy configuration, allowlists, access codes, OCR review, and offline vote entry.

## Local Development

```bash
cd apps/admin
pnpm install
pnpm dev
```

Ensure the API service is running to supply dashboard metrics and election management endpoints.
