apiVersion: v1
kind: Service
metadata:
  name: {{ include "observernet-api.fullname" . }}
  labels:
    {{- include "observernet-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "observernet-api.selectorLabels" . | nindent 4 }}
