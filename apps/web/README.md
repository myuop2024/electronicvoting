# ObserverNet Public Web App

Next.js 15 application delivering public election microsites, verification flows, WhatsApp deep links, and vote receipt lookup.

## Local Development

```bash
cd apps/web
pnpm install
pnpm dev
```

API calls are proxied through `/api` routes. Configure environment variables using the repository `.env.example` template.
